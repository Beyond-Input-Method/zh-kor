name: 中韩
author: Adrian
version: 1.0.0
description: 雾松拼音 + 中韩 方案
url: https://github.com/Beyond-Input-Method/zh-kor
createTime: 2025-05-27
updateTime: 2025-05-27
# supportPlatforms:
# preview: ""
# dirPath: ""

# 方案使用主题
use_theme:
  # 如果没有自定义主题，则使用默认主题.
  # 右边的值是主题的目录名，也可以是方案下的主题名。需要绝对路径。
  #例如: 在方案目录下，rime_ice/themes/default 是默认主题。
  # 在主题目录下，themes/default 是自定义主题。
  default: "themes/default"
  # 自定义主题
  custom:
    # 自定义主题的名称，路径同上。
    "bim_t9": "themes/t9"
    "hangul": "zh-kor/themes/Korean"
