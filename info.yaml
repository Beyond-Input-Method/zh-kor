name: 中韩
author: iDvel
version: 1.0.0
description: 方案主体来源雾松拼音
url: https://github.com/Beyond-Input-Method/rime-ice/tree/main
createTime: 2023-03-30
updateTime: 2023-03-30
# supportPlatforms:
# preview: ""
# dirPath: ""

# 方案使用主题
use_theme:
  # 如果没有自定义主题，则使用默认主题.
  # 右边的值是主题的目录名，也可以是方案下的主题名。需要绝对路径。
  #例如: 在方案目录下，rime_ice/themes/default 是默认主题。
  # 在主题目录下，themes/default 是自定义主题。
  default: "themes/default"
  # 自定义主题
  custom:
    # 自定义主题的名称，路径同上。
    "bim_t9": "themes/t9"
    "hangul": "themes/Korean"